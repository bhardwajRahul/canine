<div class="flex items-center gap-3">
  <div class="text-2xl">
    <%= cluster_icon(item.cluster) %>
  </div>
  <div class="flex-1 min-w-0">
    <div class="font-semibold"><%= item.cluster.name %></div>
    <div class="text-sm text-base-content/60">
      <%= item.namespace %> · <span class="text-success"><%= item.status.capitalize %></span>
    </div>
  </div>
</div>
